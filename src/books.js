/**
 * @typedef {{
 * id: string,
 * name: string,
 * year: number,
 * author: string,
 * summary: string,
 * publisher: string,
 * pageCount: number,
 * readPage: number,
 * reading: boolean,
 * finished: boolean,
 * insertedAt: string,
 * updatedAt: string}[]} Books
 */

/**
 * @type {Books}
 */
const books = [];

module.exports = books;
